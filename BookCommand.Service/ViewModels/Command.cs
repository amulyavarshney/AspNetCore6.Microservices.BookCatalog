namespace BookCommand.Service.ViewModels
{
    /* Creating an enum called Command with three values: Create, Update, and Delete. */
    public enum Command
    {
        Create,
        Update,
        Delete
    }
}
